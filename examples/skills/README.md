# Skill examples

Copy a skill directory into `.ygg/skills/` for a trusted project or `~/.ygg/skills/` globally. Ygg discovers metadata but does not inject instructions automatically.

Use `/skills`, `/skills search <query>`, `/skills load <name>`, and `/skills off <name>`. Supporting text under `references/` and `templates/` is loaded lazily and only for an active skill.
