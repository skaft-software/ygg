name = "caffeinate"
version = "0.2.0"
api_version = "0.2"
description = "Keep macOS awake while Ygg owns one or more active turns"

[entrypoint]
command = "extension.py"

[capabilities]
filesystem = "none"
process = true
network = false

[contributes]
commands = ["caffeinate"]
ui = ["status"]
notifications = true
