// The fake loader exposes before_provider_headers for this fixture. API 0.3
// must refuse it because header authority belongs exclusively to the host.
export default function providerHeadersHookFixture() {}
